/**
 * Enum representing the different roles a user can have.
 * 
 * @enum {string}
 * @property {string} USER - Represents a regular user.
 * @property {string} ADMIN - Represents an administrator.
 */
export enum UserRoles {
    USER = "user",
    ADMIN = "admin",
}
