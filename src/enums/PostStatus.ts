/**
 * Enum representing the status of a post.
 * 
 * @enum {string}
 * @property {string} ACTIVE - Indicates that the post is active.
 * @property {string} INACTIVE - Indicates that the post is inactive.
 */
export enum PostStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
}